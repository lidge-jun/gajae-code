# 040_interview — code facts (jwc_patched)

| 계층 | patched 처리 |
|---|---|
| L1 표면 | `jaw-interview`, `interview` command |
| L2 state | `normalizeWorkflowSkillSlug()` read-compat |
| L3 RPC | `"jaw-interview"` 송신 + `"deep-interview"` 수용 |
| runtime | `jaw-interview-runtime.ts`, gate broker 양값 |
