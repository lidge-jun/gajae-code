# 010_shell — code facts (jwc_patched)

| 항목 | patched |
|---|---|
| 공개 bin | `jwc` 추가 + `gjc` 공존 |
| wrapper | `packages/jwc` → coding-agent delegate |
| 브랜드 분기 | `APP_NAME !== "gjc"` 조건으로 jaw 표면 |
| rebrand 가드 | `jwc`를 allowed bins/packages에 추가 |
| README | `README.jwc.md` 신설(업스트림 README 무수정) |
