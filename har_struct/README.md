# har_struct/

> 각 문서 >= 100 lines

See INDEX.md


## 부록 A1

- har_struct는 `structure/`(patched SoT)와 devlog MOC를 병행한다. 갱신: 포크 완료 시 jwc_patched 선행.
- 교차: `devlog/_plan/260612_jawcode_fork/000_roadmap.md`, `AGENTS.md`(수정 금지).
- evidence: absolute path + line (structure/conventions.md).


## 부록 A2

- har_struct는 `structure/`(patched SoT)와 devlog MOC를 병행한다. 갱신: 포크 완료 시 jwc_patched 선행.
- 교차: `devlog/_plan/260612_jawcode_fork/000_roadmap.md`, `AGENTS.md`(수정 금지).
- evidence: absolute path + line (structure/conventions.md).


## 부록 A3

- har_struct는 `structure/`(patched SoT)와 devlog MOC를 병행한다. 갱신: 포크 완료 시 jwc_patched 선행.
- 교차: `devlog/_plan/260612_jawcode_fork/000_roadmap.md`, `AGENTS.md`(수정 금지).
- evidence: absolute path + line (structure/conventions.md).


## 부록 A4

- har_struct는 `structure/`(patched SoT)와 devlog MOC를 병행한다. 갱신: 포크 완료 시 jwc_patched 선행.
- 교차: `devlog/_plan/260612_jawcode_fork/000_roadmap.md`, `AGENTS.md`(수정 금지).
- evidence: absolute path + line (structure/conventions.md).


## 부록 A5

- har_struct는 `structure/`(patched SoT)와 devlog MOC를 병행한다. 갱신: 포크 완료 시 jwc_patched 선행.
- 교차: `devlog/_plan/260612_jawcode_fork/000_roadmap.md`, `AGENTS.md`(수정 금지).
- evidence: absolute path + line (structure/conventions.md).


## 부록 A6

- har_struct는 `structure/`(patched SoT)와 devlog MOC를 병행한다. 갱신: 포크 완료 시 jwc_patched 선행.
- 교차: `devlog/_plan/260612_jawcode_fork/000_roadmap.md`, `AGENTS.md`(수정 금지).
- evidence: absolute path + line (structure/conventions.md).


## 부록 A7

- har_struct는 `structure/`(patched SoT)와 devlog MOC를 병행한다. 갱신: 포크 완료 시 jwc_patched 선행.
- 교차: `devlog/_plan/260612_jawcode_fork/000_roadmap.md`, `AGENTS.md`(수정 금지).
- evidence: absolute path + line (structure/conventions.md).


## 부록 A8

- har_struct는 `structure/`(patched SoT)와 devlog MOC를 병행한다. 갱신: 포크 완료 시 jwc_patched 선행.
- 교차: `devlog/_plan/260612_jawcode_fork/000_roadmap.md`, `AGENTS.md`(수정 금지).
- evidence: absolute path + line (structure/conventions.md).


## 부록 A9

- har_struct는 `structure/`(patched SoT)와 devlog MOC를 병행한다. 갱신: 포크 완료 시 jwc_patched 선행.
- 교차: `devlog/_plan/260612_jawcode_fork/000_roadmap.md`, `AGENTS.md`(수정 금지).
- evidence: absolute path + line (structure/conventions.md).


## 부록 A10

- har_struct는 `structure/`(patched SoT)와 devlog MOC를 병행한다. 갱신: 포크 완료 시 jwc_patched 선행.
- 교차: `devlog/_plan/260612_jawcode_fork/000_roadmap.md`, `AGENTS.md`(수정 금지).
- evidence: absolute path + line (structure/conventions.md).


## 부록 A11

- har_struct는 `structure/`(patched SoT)와 devlog MOC를 병행한다. 갱신: 포크 완료 시 jwc_patched 선행.
- 교차: `devlog/_plan/260612_jawcode_fork/000_roadmap.md`, `AGENTS.md`(수정 금지).
- evidence: absolute path + line (structure/conventions.md).


## 부록 A12

- har_struct는 `structure/`(patched SoT)와 devlog MOC를 병행한다. 갱신: 포크 완료 시 jwc_patched 선행.
- 교차: `devlog/_plan/260612_jawcode_fork/000_roadmap.md`, `AGENTS.md`(수정 금지).
- evidence: absolute path + line (structure/conventions.md).


## 부록 A13

- har_struct는 `structure/`(patched SoT)와 devlog MOC를 병행한다. 갱신: 포크 완료 시 jwc_patched 선행.
- 교차: `devlog/_plan/260612_jawcode_fork/000_roadmap.md`, `AGENTS.md`(수정 금지).
- evidence: absolute path + line (structure/conventions.md).


## 부록 A14

- har_struct는 `structure/`(patched SoT)와 devlog MOC를 병행한다. 갱신: 포크 완료 시 jwc_patched 선행.
- 교차: `devlog/_plan/260612_jawcode_fork/000_roadmap.md`, `AGENTS.md`(수정 금지).
- evidence: absolute path + line (structure/conventions.md).
