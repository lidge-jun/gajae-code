# 085.6 — 아이덴티티 누수 제로: jwc 세션 "나는 GJC" 응답 차단

> 상위: [085_moc_workflow_hud.md](./085_moc_workflow_hud.md). 레이어 엔진: [085.5](./085.5_plan_prompt_rebrand.md) L1/L2. identity 표면: [021](./021_plan_identity_prompting.md)(구현 완료). 스킬 주입 연계: [057](./057_plan_skill_compat_patch.md) P10/stage-skill-map.
> 트리거: 260612 01:16PM 실측 — jwc TUI 세션 "너는 누구야" → **"나는 GJC, Gajae Code 코딩 에이전트야"** + thinking이 "gajae-code-system-prompt" 자기참조 + 응답 헤더 라벨 "gajae".
> 원칙: 085.5와 동일 — **소스 .md는 gjc diff-0, 전환은 placeholder/런타임 레이어**. identity 미설정 시에도 jwc 브랜드면 GJC 정체성이 노출되면 안 됨.

## 0. 누수 경로 전수 (실측 — 260612 01:2x 코드 확인)

| # | 누수 | 근원 (file:line) | 수정 레벨 |
|---|------|------------------|-----------|
| N1 | 정체성 문장 "You are GJC, the Gajae Code coding agent" | `packages/coding-agent/src/prompts/system/system-prompt.md:3` — `buildSystemPrompt` data에 APP_NAME 부재 (085.5 §0-1) | 시스템 프롬프트 단 (A) |
| N2 | 래퍼 태그 `<gajae-code-system-prompt>` — thinking의 자기참조 근원 | `system-prompt.md:1,275` | 시스템 프롬프트 단 (A) |
| N3 | identity 블록이 **추가**일 뿐 **교체**가 아님 — L3 "You are GJC"와 공존하면 모델이 첫 문장(L3) 채택, identity 미설정이면 블록 자체 부재 | `system-prompt.ts:283` `renderIdentityBlock` → `:588` customization 합성 (021 구조) | 주입 단 (B) |
| N4 | TUI 응답 헤더 "gajae" 하드코드 | `modes/components/assistant-message.ts:19` | 표시 단 (C) |
| N5 | 상태라인 segment "gajae" — id는 [기능](스키마·preset 키), **렌더 텍스트만** 전환 대상 | `status-line/segments.ts:74`, `presets.ts:36,60`, `settings-schema.ts:72` | 표시 단 (C) |
| N6 | 기타 산문 GJC (L21·42·225, tools/bash.md:20, agents md) | 085.5 인벤토리 1a | 085.5 M2/M3 소관 — 본 플랜 무중복 |

## 1. 설계 — 정체성 해석 단일 정본

**우선순위 체인 [확정 제안]**: `identity.name`(021 설정) → **jwc 브랜드 표시명** → `GJC`(gjc 브랜드 전용).

핵심은 N3 해소 방식: identity 블록을 강화(override 문구 추가)하는 게 아니라, **L3 정체성 문장 자체가 체인을 직접 받게** 한다 → 충돌 자체가 소멸. identity 블록(021)은 vibe/언어 담당으로 유지, 무변경.

### A. 시스템 프롬프트 단 (085.5 L1 승차 — handlebars data)

- `system-prompt.md:3` → `You are {{AGENT_DISPLAY_NAME}}, the {{PRODUCT_DISPLAY_NAME}} coding agent.`
- `system-prompt.md:1,275` 래퍼 태그 → `<{{SYSTEM_PROMPT_TAG}}>` (jaw 브랜드 `jawcode-system-prompt`, gjc `gajae-code-system-prompt`)
- data 주입: `buildSystemPrompt` data(`system-prompt.ts:593-619` 부근)에 신규 헬퍼 값 3종 추가 — **gjc 브랜드 실행 시 산출 byte-동일** (AGENT_DISPLAY_NAME=GJC 등)이라 diff-0 불변식 유지 (021·010 패턴 승계)

### B. 주입 단 (런타임 헬퍼 — 단일 정본 모듈)

- 신규 `gjc-runtime/agent-identity.ts` (≤50줄, 순수):
  - `resolveAgentDisplayName(): string` — `identity.name`(settings, throwing-Proxy try/catch 가드 — 021 §감사3 패턴) → jaw 브랜드 표시명 → `"GJC"`
  - `resolveProductDisplayName(): string` — jaw 브랜드 표시명 ↔ `"Gajae Code"`
- 085.5 L2 `brandPromptText()`·057 dev 어휘 맵·stage-skill-map 주입 블록(057 P10)이 **모두 이 헬퍼를 공용** — 새 주입 텍스트가 새 누수원이 되지 않게 단일 정본
- cli-jaw 임베딩(M2 130)에서는 cli-jaw `settings.json`의 identity(Jaw 🦈)를 `identity.*`로 시딩하면 체인 1순위로 자연 주입 — 본 플랜은 통로만 보장, 시딩은 130 소관

### C. 표시 단 (TUI)

- `assistant-message.ts:19` 헤더: `"gajae"` 리터럴 → `resolveAgentDisplayName()` 소문자 표기 (gjc 브랜드 byte-동일)
- `segments.ts:74` gajae 세그먼트 **렌더 텍스트**: 동일 헬퍼 — segment **id·스키마 키는 무변경** (settings-schema.ts:72 유니온, preset 호환·마이그레이션 회피)

## 2. 구현 모듈

| M | 내용 | 파일 | 의존 |
|---|------|------|------|
| M1 | `agent-identity.ts` 헬퍼 + 단위 테스트 | 신규 `gjc-runtime/agent-identity.ts` | 없음 — 즉시 가능 |
| M2 | N1·N2 placeholder + buildSystemPrompt data 확장 | `prompts/system/system-prompt.md:1,3,275`, `system-prompt.ts` data | M1, 085.5 M2와 같은 커밋 권장 |
| M3 | N4·N5 TUI 표시 전환 | `modes/components/assistant-message.ts:19`, `status-line/segments.ts:74` | M1 |
| M4 | 테스트 — 아래 표 | 신규 `test/agent-identity-leak.test.ts` | M1–M3 |

### M4 테스트 표

| 케이스 | 기대 |
|--------|------|
| gjc 브랜드(env unset) buildSystemPrompt | 기존과 **byte-동일** (diff-0) |
| jwc 브랜드 + identity 미설정 | 산출 전문에 `GJC`·`Gajae` **비노출** (래퍼 태그 포함), 표시명으로 치환 |
| jwc 브랜드 + `identity.name="Jaw"` | `You are Jaw, …` + 021 identity 블록 공존 무충돌 |
| settings 미초기화(SDK/임베딩 경로) | throw 없이 브랜드 표시명 폴백 |
| segments id/스키마 | `"gajae"` 키 유지 (preset 로드 무회귀) |
| e2e 수동 | jwc TUI "너는 누구야" → GJC/Gajae 비언급, 표시명/identity로 답변 |

게이트: `bun run check:ts` + rebrand 가드(G002 무접촉 — defaults 스킬 디렉터리 비대상) + `default-gjc-definitions.test.ts`(번들 스킬 무접촉) + 085.5 diff-0 스냅샷과 병행 green.

## 3. [열린 질문 → 확정] (인터뷰 260612 01:36)

1. **jwc 브랜드 표시명 [확정 — 사용자]**: 에이전트 정체성은 **`Jaw`**, 자기 인지는 **"jwc 런타임(Jawcode) 위에서 돈다"**. jwc 산문 정체성 문장: `You are Jaw, the coding agent running on the jwc runtime (Jawcode).` — 즉 AGENT_DISPLAY_NAME 기본값(identity.name 미설정 시) = `Jaw`, 런타임/제품명 = `jwc`/`Jawcode`, 명령 구문 APP_NAME = `jwc` 유지. 085.5 열린 질문 1도 동일 결정 적용. gjc 브랜드는 기존 문장 byte-동일.
2. 래퍼 태그 전환(N2): **전환 O** [확정 — 기본값 수용] — gjc 무변경이라 무위험, jwc만 신규 태그.
3. role-agent 스폰 프롬프트의 "GJC" 산문 — 085.5 M3 `brandPromptText()` 소관 재확인만. spawn 시점 정체성 1줄 주입은 057 §6 P10(M5-2)과 동일 resolve 경유로 확정.

## 4. 구현 순서·연계

**M1 → M2(085.5 M2와 동행) → M3 → M4.** 085.5 M1(기능 버그)과 독립이라 병렬 가능.
스킬 주입 플랜(057 P10 + stage-skill-map, 260612 01:13PM 설계)과의 계약: 주입 블록 생성부는 반드시 M1 헬퍼·085.5 `brandPromptText()`를 경유 — 주입 텍스트의 GJC/cli-jaw 어휘 누수를 같은 레이어에서 차단.
