# 99.00.00 MOC — jwc 안정화 마스터플랜 (99 밴드)

> 상태: 🟡 밴드 개설 (260612 저녁) — 잔여 작업 전수를 `99.GG.NN` 단일 시퀀스로 통합 추적.
> 입력: 사용자 "남은 작업을 99.00.01부터 설계해놓자, jwc 안정화를 위해 할 게 많다" +
> "미구현 사항 전부, 시스템 프롬프트 지침도 같이" + "70번대 문서부터 99로 마이그레이션" (260612).

미구현 밴드(070 메모리, 085 HUD)와 신규 진단(pabcd discovery 브리지·워크플로 표면 전면 개정),
이월 마감분을 한 밴드에서 순차 실행 가능하게 묶는다. 구 밴드 문서는 99 하위로 **마이그레이션**하고
원본 자리에는 리다이렉트 스텁만 남긴다 (병렬 세션의 구 경로 참조 보호).

## 번호 규약

- `99.GG.NN` — `GG` = 작업 패키지, `NN` = 패키지 내 문서 일련 (`.00` = 패키지 MOC).
- 번호는 식별자일 뿐, **착수 순서의 정본은 본 MOC의 착수 순서표**.

## 작업 패키지

| GG | 패키지 | 출처 | 상태 |
|----|--------|------|------|
| [99.01](./99.01.00_moc_memory.md) | 메모리 병합 — `jwc memory search/read/save/context` + `jwc chat search` | 070~072 마이그레이션 | 설계 ✅ / 구현 ⬜ |
| [99.02](./99.02.00_plan_workflow_surface_revision.md) | pabcd discovery 브리지 + 워크플로 표면 전면 개정 (M1 시스템 프롬프트 · M2 매 턴 헤더 · M3 자가 전이 · 명칭 re-facing) | 신규 (260612 진단) | 조사 ✅(8기) / 설계 v1 / 결정 2건 대기 |
| 99.03 | 워크플로 HUD (pabcd 띠·인터뷰 게이지·goal 세그먼트·인프라) — 마이그레이션 시 `.gjc/`→`.jwc/` 경로 표기 전면 정정 + verify 게이트 신설로 전환 | 085.1~085.4 마이그레이션 예정 | 설계 ✅ / 구현 ⬜ |
| 99.04 | 병렬 랜딩 정리 — 083.5 커밋 · 060 M4(TUI /goal 별칭) · 테스트 그린(ask.test, state-read-markdown) | 이월분 | 코드 ✅ / 마감 ⬜ |
| 99.05 | auth 릴리즈 게이트 | 090 | MOC만 |
| 99.06 | 문서 정합 스윕 — stale 상태 헤더 일괄 갱신 (094.1~3, 085.5/085.6, 056 goal stub 등) | 유지보수 | ⬜ |
| 99.07 | 슬래시/CLI 패리티 보강 — `/goal` 의미 분기 해소(ledger 동사 부재), `/interview`·`/plan`·`/team` 슬래시 부재, 세션 관리 슬래시군(/sessions·/switch·/fork), `orchestrate reset` 동사 | 신규 (260612 패리티 조사) | 조사 ✅ / 설계 ⬜ |

## 착수 순서 (사용자 확정 260612)

`99.04 정리 → 99.01 메모리 → 99.02 브리지 → 99.03 HUD → 99.05 → 99.06`

- 99.04가 dirty 39건을 청산해야 잠자는 β goal(아래 제외 항목)의 게이트도 열린다.
- 99.02는 병렬 조사(8기) 종합 후 플랜 문서 작성 → 사용자 승인 → 구현.

## 제외 (이중 추적 방지)

- **069.1 Phase β(P9~P11) + P8(har_struct)**: paused goal `3f6989ac`(`~/.cli-jaw/goal/active.json`) 소유.
  99에서는 게이트 의존성으로 참조만 — 재개는 cli-jaw goal resume.

## 결정 (260612 인터뷰)

1. [확정] β goal은 99 밴드에 흡수하지 않고 별도 유지.
2. [확정] 시스템 프롬프트 지침은 **워크플로 표면 전면 개정** — 주입 명칭도 ralplan 등 gjc 유산 어휘가 아니라
   **pabcd 명칭 규격**에 맞춰 재편 (re-facing 설계는 99.02 조사 종합 후).
3. [확정] pabcd 매 턴 재주입(M2) 채택 여부는 gjc·cli-jaw 양쪽 주입 구조 실측 후 결정 → 조사 완료분:
   cli-jaw는 매 턴 `getPrefix()` 헤더 + A/B/C 매 턴 plan 재주입(push), 스킬 본문은 pull.
   jwc는 plan/goal 모드 custom-message 레일이 동형 확장점.
