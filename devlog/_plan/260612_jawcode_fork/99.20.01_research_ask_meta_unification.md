# 99.20.01 — research: 선다형 ask 입력 UX 통일 (meta 경로 전역화)

> 상태: 🟡 조사 완료 (260612 밤, Sonnet 서브에이전트 코드 분석) — 방향 확정, 구현 ⬜.
> 소속: [99.20.00 MOC](./99.20.00_moc_frontend_uiux.md). 관련: [082.3 v2](./082.3_plan_interview_ask_docked_input.md) ✅.
> 결정: **[확정 260612 — 사용자] 선다형 ask는 어느 상황에서든 meta(v2) 입력 UX로 통일** —
> "선다로 내는 걸 meta로 하도록, 어느 상황에서든. 이게 UX 통일성에 맞다."
> 증거: Screenshot 2026-06-12 at 10.10.32 PM.png — 인터뷰 세션 중 meta 누락 ask가
> 번호 없는 옵션 + 레거시 `Other (type your own)` 2단계 경로로 렌더된 실측.

082.3 v2(번호 옵션 + 리스트 마지막 슬롯 인라인 composer 입력)는 완성됐지만, 활성 게이트가
`isStructuredInterviewQuestion(q.meta)` 단일 판정이라 모델이 meta를 빼먹으면 같은 선다형
질문이 전혀 다른 UX(레거시 Other 2단계)로 떨어진다. 같은 인터랙션이 호출 파라미터 하나로
두 얼굴을 갖는 것 — 본 조사는 이 게이트를 풀어 **모든 single-select ask가 v2 UX**가 되도록
하는 변경의 전모를 코드 전수 분석으로 확정했다. 082.3 §5의 "일반 ask Other 회귀 없음" 비목표는
본 결정으로 **폐기**된다 (일반 ask도 v2로 끌어올림).

핵심 한 줄: **`ask.ts:576`의 `shouldNumberOptions = jawInterviewPrompt !== null`을
`!(q.multi ?? false)`로 교체하는 것이 런타임 변경의 전부.** 인터뷰 헤더(라운드 띠)는 meta
없이는 만들 수 없으므로 기존 meta 게이트를 유지 — "입력 UX"와 "인터뷰 헤더"의 게이트 분리가
설계의 본질이다. 작업 부하의 80%는 `ask.test.ts`의 레거시 패턴 교체.

## 1. 호출부 인벤토리 (260612 분석)

| 위치 | 내용 |
|------|------|
| `ask.ts:104` | `OTHER_OPTION = "Other (type your own)"` — 레거시 sentinel |
| `ask.ts:257-274` | `useDockedCustomInput` true → `customInputListSlot + listSlotCustomInput`, false → `customInput: { optionLabel }` |
| `ask.ts:389-391` | single 경로 — Other 행 삽입 여부 (`useDockedCustomInput ? displayLabels : [...displayLabels, otherOptionLabel]`) |
| `ask.ts:426` | Other 선택 처리 분기 |
| `ask.ts:572-601` | **핵심 게이트** — `jawInterviewPrompt !== null`에 v2 동작 전부 종속 |
| `ask.ts:802` | `isStructuredInterviewQuestion` 2차 사용 — multi-question **히스토리 렌더 전용** (UI 경로 아님, 본 변경 무관) |
| `hook-selector.ts:75-99` | 커스텀 입력 4모드 인터페이스 (`customInput`/`customInputDocked`/`customInputListSlot`/`listSlotCustomInput`) |
| `hook-selector.ts:465-468` | 생성자 분기 — listSlot → `#mountOutputPanelChrome()`, docked → `#mountDockedInputArea()` |
| `extension-ui-controller.ts:35` | `OTHER_OPTION` 선언만 있고 **참조 0 — dead code** (정리 대상) |
| `jaw-interview-gate.ts:18` | `GATE_OTHER_OPTION` — RPC 게이트 전용, UI와 완전 독립 (무변경) |

범위 외: `session-selector.ts`·`selector-controller.ts`·`runtime-mcp-command-controller.ts` 등
앱 내부 메뉴는 customInput 없는 단순 선택 — 본 건 비대상 (단, 불일치 표면 추가 조사 별건 진행 중).

## 2. 게이팅 체인 — meta 의존성 분해

`ask.ts:572` `jawInterviewPrompt !== null`에 걸린 동작별로 meta 필요 여부가 갈린다:

| 동작 | 위치 | meta 없이 가능? | 처분 |
|------|------|----------------|------|
| (a) 번호 부여 `numberOptionLabels` | `ask.ts:576,580` | ✅ 가능 | **전역화** |
| (b) 헤더 `formatInterviewSelectorPrompt` | `ask.ts:575` (`structured-renderer.ts:61-78`) | ❌ `meta.kind/round/component` 직접 접근 | meta 게이트 유지 |
| (c) `scrollTitleRows` | `ask.ts:600` | 가능하나 헤더 없이는 무의미 | meta 게이트 유지 |
| (d) v2 슬롯 `useDockedCustomInput` | `ask.ts:601` | ✅ 가능 | **전역화** |
| (e) initialSelection 라벨 재매핑 | `ask.ts:578-587` | ✅ (a)에 종속 | 자동 동반 |
| (f) selectedOptions 역매핑 | `ask.ts:603-608` | ✅ (a)에 종속 | 자동 동반 |

## 3. 확정 설계

```ts
// ask.ts:576 — 변경 전
const shouldNumberOptions = jawInterviewPrompt !== null;
// 변경 후: 모든 single-select에 번호+v2 슬롯
const shouldNumberOptions = !(q.multi ?? false);
```

- `displayQuestion = jawInterviewPrompt ?? q.question` — **유지** (헤더는 meta 게이트)
- `scrollTitleRows` — `jawInterviewPrompt !== null` 게이트 **유지**
- multi-select — Other 행 + 레거시 경로 **현행 유지** (D082-3-6 승계; v2 multi는 추후 별건)
- `extension-ui-controller.ts:35` dead `OTHER_OPTION` 제거
- `hook-selector.ts`·`jaw-interview-gate.ts` — **무변경**

## 4. 테스트 영향 (`ask.test.ts` 집중)

수정 필요: `"leaves plain questions without meta unstructured and unnumbered"`(~:1243, 전면 재작성 —
기대값이 본 결정의 정반대), Other 반환 패턴 6건(~:255, ~:394, ~:424, ~:487, ~:765-854, ~:1004) →
`listSlotCustomInput.onSubmit` 시뮬레이션으로 교체.
무변경: `hook-selector-list-slot.test.ts`, `hook-selector-inline-input.test.ts`,
`jaw-interview-gate-redteam.test.ts`, `jaw-interview-workflow-gates.test.ts`.

## 5. 리스크

| 리스크 | 심각도 |
|--------|--------|
| `ask.ts:399` initialIndex 오프셋 — `customInput !== undefined`면 `displayLabels.length`인데 Other 행 소멸 시 범위 초과 (저장된 customInput으로 재방문) | **중간 — 구현 시 필수 수정** |
| 웹/브릿지 표면이 `"Other (type your own)"` 라벨 존재를 가정할 가능성 | 중간 (구현 전 확인) |
| 빈 텍스트 처리 차이 — v2 슬롯은 `!text` 차단(`hook-selector.ts:653`), 레거시는 `""`도 customInput | 낮음 (skip 정책이 정답 — 082.3 D082-3-5) |
| timeout 자동선택 / RPC 게이트 디코딩 / multi-select | 없음 (각각 rawLabels 기준·`other: true` 플래그 기반·비대상) |

## 6. 변경 규모

| 파일 | 내용 | LOC |
|------|------|-----|
| `src/tools/ask.ts` | 게이트 교체 + initialIndex 수정 | ~10 |
| `src/modes/controllers/extension-ui-controller.ts` | dead code 제거 | -1 |
| `test/tools/ask.test.ts` | 레거시 패턴 → v2 시뮬레이션 교체 | ~150-200 |

총 3파일 / ~160-210 LOC.

## 다음 액션

- [ ] 구현 착수 (§3) — initialIndex 리스크 동시 수정
- [ ] 웹/브릿지 `OTHER_OPTION` 가정 여부 1회 확인
- [ ] 082.3 §5 비목표 폐기 주석 + 본 문서 참조 기입
- [ ] 불일치 표면 추가 조사 결과 합류 시 후속 NN 분배 ([99.20.00](./99.20.00_moc_frontend_uiux.md))
