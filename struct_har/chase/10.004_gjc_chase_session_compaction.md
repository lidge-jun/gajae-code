# 10.004 — gjc chase: 세션 · compaction · harness

> MOC: [10_gjc_chase_MOC](./10_gjc_chase_MOC.md) · [bands/083_output](./bands/083_output.md) · G1 · ⬜ · **P1**

GJC dev `050aa1731551` adds pre-send estimated context maintenance and canonical tool-output pruning persistence. This is correctness-critical because a large tool result appended after the last assistant turn can otherwise push the next model request over the context window.

| 파일 | fork-delta | source facts |
|---|---|---|
| `packages/coding-agent/src/session/agent-session.ts` | HARD-EDIT | upstream checks the last assistant and then runs `#checkEstimatedContextBeforePrompt()` before building the next message array (`devlog/_upstream_gjc/.../agent-session.ts:4747-4756`). |
| same | HARD-EDIT | upstream threshold flow prunes tool outputs at a sanctioned maintenance boundary, subtracts saved tokens, then compacts/promotes only if still needed (`:6517-6533`, `:6537-6558`). |

jaw 083 segment·minimize·collapse 선행 구간과 겹치므로 코드 포팅은 3-way 수동 병합만 허용한다. 특히 provider prompt-cache epoch 중 이미 보낸 toolResult history를 함부로 다시 쓰지 않는 invariant를 유지해야 한다.

## 완료

- [ ] GJC `050aa1731551` 기준 pre-send context maintenance 동등성 판정
- [ ] canonical prune persistence가 jwc session storage와 TUI replay/segment collapse를 깨지 않는지 확인
- [ ] CHANGELOG 표 (10.001 연동) → `_legacy/10/`