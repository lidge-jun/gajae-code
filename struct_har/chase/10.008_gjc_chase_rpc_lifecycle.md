# 10.008 — gjc chase: RPC lifecycle · `get_state` payload

> MOC: [10_gjc_chase_MOC](./10_gjc_chase_MOC.md) · G1 · ⬜ · P2
> Reviewed source: GJC `050aa1731551` vs JWC `a771f492d382`.

## 왜 gap인가

Upstream GJC dev hardened headless RPC behavior after the previous chase baseline. The changes affect long-lived embedding clients and workflow-control surfaces: malformed JSONL no longer kills the session, EOF/shutdown flushes durable state before exit, pending host bridges are rejected/cleared, and default `get_state` output no longer dumps static tools/systemPrompt unless explicitly included.

## Source facts

| fact | upstream evidence |
|---|---|
| shutdown rejects pending host tool calls and clears pending host URI requests | `devlog/_upstream_gjc/packages/coding-agent/src/modes/rpc/rpc-mode.ts:234-241` |
| shutdown calls `session.sessionManager.ensureOnDisk()` before dispose/exit | `rpc-mode.ts:241-249` |
| JSONL stdin is parsed frame-by-frame; malformed frames emit parse-error responses and continue | `rpc-mode.ts:526-539` |
| regular command errors also return parse command errors without tearing down the loop | `rpc-mode.ts:541-576` |
| EOF routes through shutdown, so disconnected clients still get durable state flush | `rpc-mode.ts:579-580` |
| changelog says default RPC `get_state` omits static `dumpTools` and `systemPrompt` unless requested with `include: ["tools", "systemPrompt"]` | `devlog/_upstream_gjc/packages/coding-agent/CHANGELOG.md:10` |
| changelog says RPC stdio has red-team coverage for attached persistence, reload, malformed-frame recovery, and concurrent child-session isolation | `CHANGELOG.md:14` |

## JWC reconcile notes

- Keep JWC workflow-gate/orchestrate naming and `.jwc` state paths; do not copy upstream `.gjc` user-facing strings blindly.
- Verify whether JWC already has the malformed-frame and EOF persistence behavior before porting; the source paths are likely HARD-EDIT around RPC/workflow gates.
- The `get_state` payload reduction needs implementation-source inspection outside this card's initial focus before marking complete.
- The value is operational reliability and token/payload pressure, not visible TUI polish.

## 완료 기준

- [ ] JWC RPC mode compared against the source facts above.
- [ ] `get_state` include-gated tools/systemPrompt behavior confirmed or filed as a separate implementation task.
- [ ] Focused RPC tests cover malformed JSONL continuation, EOF durability, shutdown bridge cleanup, and `get_state` include behavior.
